name: Commit e PR - Principal

on:
  pull_request:
    branches: [principal]

jobs:
  testes-linguagem:
    runs-on: ubuntu-latest

    steps:
        with:
          node-version: '16'
      - name: NPM - Dependências
        run: |
          sudo npm install
      - name: Testes de liquido
        run: |
          sudo echo Funcionou

# Terminar de escrever essa parte do codigo.
